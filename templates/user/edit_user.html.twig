{% extends 'base.html.twig' %}

{% block body %}
    <div class="content">

        <h1>User</h1>
        {{ user.id }} - {{ user.firstname }}

        <h1>Update ✅</h1>

        <br>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

        <a href="{{ path('remove_user', {'id': user.id}) }}">Remove</a>

    </div>
{% endblock %}