{% extends 'base.html.twig' %}

{% block body %}
    <div class="content">

        <h1>Category</h1>
        {{ category.id }} - {{ category.title }}

        <a href="{{ path('edit_category', {'id': category.id}) }}">Edit</a>

    </div>
{% endblock %}
