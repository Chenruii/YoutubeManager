{% extends 'base.html.twig' %}

{% block title %}Hello categoryController!{% endblock %}

{% block body %}
    <div class="content">
        <h1>category</h1>

        <ul>
            {% for category in categories %}
                <a href="{{ path('details_categories', { 'id': category.id }) }}">{{ category.title }}</a>
                <br>

            {% endfor %}
        </ul>


    </div>

{% endblock %}
